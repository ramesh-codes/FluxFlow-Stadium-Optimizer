const express = require("express"); const FluxFlowAssistant = require("./src/assistant"); const app = express(); const port = process.env.PORT || 8080; app.get("/", (req, res) => { const result = FluxFlowAssistant.getRecommendation("G1"); res.json(result); }); app.listen(port, () => { console.log(`FluxFlow listening on port ${port}`); });
